# core (agent + local self-play; the engine ships in the Kaggle download)
numpy>=1.24
kaggle-environments==1.30.1   # same version as the ladder

# NN phase (behavioral cloning / RL) — CPU build is fine; MPS on Apple Silicon
torch>=2.2
